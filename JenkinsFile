pipeline {
    agent { docker { image 'golang' } }
    stages {
        stage('build') {
            steps {
                sh 'go version'
                sh 'cd deployments'
                sh 'ls -a'
                sh 'docker ps'
            }
        }
    }
}