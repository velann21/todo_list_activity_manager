pipeline {
    agent { docker { image 'golang' } }
    stages {
        stage('build') {
            steps {
                sh 'go version'
                sh 'cd deployments'
                sh 'export XDG_CACHE_HOME=/tmp/'
                sh 'echo $XDG_CACHE_HOME'
                sh 'ls -a'
                sh 'make build'

            }
        }
    }
}